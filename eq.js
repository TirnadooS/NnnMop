const eqSettings = {
    bass: 0,
    mids: 0,
    treble: 0
};

function initEqualizer() {
    // Здесь будет реальная обработка через Web Audio API
    console.log("Эквалайзер инициализирован");
}

// Инициализация при загрузке
window.onload = initEqualizer;
